apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "react-devops.fullname" . }}-test-connection"
  labels:
    {{- include "react-devops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "react-devops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
